services:
  maintenance:
    image: ghcr.io/blixten85/docker-idempotent-update:latest
    container_name: docker-maintenance
    pull_policy: always
    restart: unless-stopped
    environment:
      COMPOSE_DIR: /data
      COMPOSE_FILE: /data/docker-compose.yml
      RCLONE_SRC: /data
      RCLONE_DST: ${RCLONE_DST:-gdrive:backups}
      EMAIL_TO: ${EMAIL_TO:-}
      CRON_SCHEDULE: ${CRON_SCHEDULE:-0 3 * * *}
      DOCKER_CONFIG: /config/docker
      RCLONE_CONFIG: /config/rclone.conf
    security_opt:
      - no-new-privileges:true
    volumes:
      - ${DOCKER_DIR}/docker-maintenance/config:/config:ro
      - ${DOCKER_DIR}:/data:ro
      - /var/run/docker.sock:/var/run/docker.sock
